import { Typography } from '@mui/material';
import styledComponents from 'styled-components';

const TypographyContainer = styledComponents(Typography)`
  display: flex;
  align-items: center;
`;

export default TypographyContainer;
